[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA / rutas locales: servir index.html si falta el archivo
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
